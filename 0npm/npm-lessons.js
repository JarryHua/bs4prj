console.log(`
LESSON-001
* upload the project configure need a pair files
  1) pachage.json
  2) package-lock.json
  like their file names show, lock means lock specified version, otherwise
  use the current or upper compatible version.
`);